<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.scoula.mapper.notification.NotificationMapper">

    <resultMap id="notificationMap" type="org.scoula.domain.notification.NotificationVO">
        <id property="notiId" column="noti_id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="notiType" column="noti_type"
                typeHandler="org.scoula.common.typehandler.notification.NotiTypeTypeHandler"/>
        <result property="amount" column="amount"/>
        <!-- BaseTime -->
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>
    <insert id="saveNotification">
        INSERT INTO notification (user_id, title, content, noti_type, amount)
        VALUES (#{userId}, #{title}, #{content}, #{notiType}, #{amount})
    </insert>

    <!-- 사용자의 알림 목록 조회 (최신순) -->
    <select id="findAllByUserId" resultMap="notificationMap">
        SELECT *
        FROM notification
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
        LIMIT 100
    </select>

    <select id="findByNotiId" resultMap="notificationMap">
        SELECT noti_id,
               title,
               content,
               noti_type,
               amount
        FROM notification
        WHERE noti_id = #{notiId}
    </select>

</mapper>